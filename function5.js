const arr = [4, 9, 16, 25];
const sqrtArr = arr.map((num) => Math.sqrt(num));
console.log(sqrtArr); // Output: [2, 3, 4, 5]

const arr = [1, 2, 3, 4, 5];
const multipliedArr = arr.map((num) => num * 2);
console.log(multipliedArr);
